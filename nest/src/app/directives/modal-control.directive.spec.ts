import { ModalControlDirective } from './modal-control.directive';

describe('ModalControlDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalControlDirective();
    expect(directive).toBeTruthy();
  });
});
